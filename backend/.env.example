# MySQL Database Configuration
# For Docker: use DB_HOST=db (service name)
# For Local: use DB_HOST=localhost
DB_HOST=db
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_NAME=itpm_db
DB_PORT=3306

# Server Configuration
PORT=5000
NODE_ENV=development
